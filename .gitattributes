# Handle line endings consistently across platforms for C++ files
*.cpp text eol=lf
*.h text eol=lf
*.c text eol=lf
*.cs text eol=lf

# If you're using scripts or batch files, you can handle their line endings too
*.sh text eol=lf
*.bat text eol=crlf

# Treat Unreal Engine config and build files as text files for easier diffing
*.ini text
*.uproject text

# Optional: Track README, license, and other text files
*.md text
*.txt text

# Ignore Unreal Engine asset files (you won't be tracking them)
*.uasset -text
*.umap -text